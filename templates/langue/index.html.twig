{% extends 'base.html.twig' %}

{% block title %}Liste des langues{% endblock %}

{% block body %}
<div class="container">
{# langues #}
    <h1 class="text-center"> Liste des langues</h1>
    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
      {% for lang in languages %}
        <ul>
            <li><a href="{{path('lang_course', {id: lang.id})}}">{{lang.name}}</a></li>
            {# <li><a href="{{path('lang_course')}}">{{lang.name}}</a></li> #}

        </ul>
      {% endfor %}    
    </div>
{# coours affiché selon la langue sélectionnée #}
    {# <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
      {% for course in coursesCollection %}
        {% include 'index/_course-card.html.twig' with {course.course} %}
      {% endfor %}    
    </div> #}
</div>
{% endblock %}
